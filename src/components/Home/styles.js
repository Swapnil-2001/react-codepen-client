import { makeStyles } from "@mui/styles";

export default makeStyles(() => ({
  box: {
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
    flexDirection: "column",
  },
}));
